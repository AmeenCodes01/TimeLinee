import Login from "@/features/Login-card";

const Auth = () => {
  return (
    <div className="w-full h-full justify-center items-center flex">
      <Login />
    </div>
  );
};

export default Auth;
